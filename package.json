{
  "name": "pdf",
  "version": "0.1.0",
  "description": "PDF toolkit CLI (scaffold)",
  "type": "module",
  "bin": {
    "pdf": "./bin/pdfkit.js"
  },
  "scripts": {
    "start": "node ./bin/pdfkit.js --help",
    "lint": "eslint .",
    "format": "prettier -w .",
    "test": "node scripts/smoke-merge-rotate.js",
    "cli": "node --enable-source-maps ./bin/pdfkit.js --help"
  },
  "engines": {
    "node": ">=18"
  },
  "keywords": [
    "pdf",
    "cli"
  ],
  "license": "MIT",
  "dependencies": {
    "commander": "^12.1.0",
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "eslint": "^9.20.1",
    "prettier": "^3.5.2"
  }
}
